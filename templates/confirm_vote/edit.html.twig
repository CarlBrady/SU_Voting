{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit ConfirmVote{% endblock %}

{% block body %}
    <br>
    <h2>Edit Scheduled Votes</h2>
    <br>
    <table>
     {{ include('confirm_vote/_form.html.twig', {'button_label': 'Update'}) }}
     {{ include('confirm_vote/_delete_form.html.twig') }}
    </table>
    <a href="{{ path('confirm_vote_index')}}"><button class="btn btn-outline-info">{{ button_label| default ('Back') }}</button></a>
{% endblock %}
