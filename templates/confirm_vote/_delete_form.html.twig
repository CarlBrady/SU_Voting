<form method="post" action="{{ path('confirm_vote_delete', {'id': confirm_vote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirm_vote.id) }}">
    <button class="btn-danger btn-lg">Delete</button>
</form>
